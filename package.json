{
  "name": "lunchcor",
  "private": true,
  "scripts": {
    "dev": "npm --prefix src run dev",
    "build": "npm --prefix src run build",
    "start": "npm --prefix src run start",
    "typecheck": "npm --prefix src run typecheck",
    "check": "npm --prefix src run check",
    "docker:build-and-start": "powershell -ExecutionPolicy Bypass -File ./scripts/build-and-start.ps1",
    "docker:build-and-start:sh": "./scripts/build-and-start.sh"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "nuxt-webhook-validators": "^0.2.8",
    "playwright": "^1.62.1"
  }
}
