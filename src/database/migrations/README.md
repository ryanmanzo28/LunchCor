# Migration Folder Consolidated

SQL migration files were consolidated into `database/init` at the repository root.

Use `database/init` as the single source of truth for database migration/init SQL.

This folder remains for structure compatibility only.
